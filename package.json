{
  "scripts": {
    "client": "npm start --prefix client",
    "server": "npx json-server --watch server/db.json --port 3001",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}